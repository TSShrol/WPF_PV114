<Window x:Class="_04_1_Binding_INotify.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_04_1_Binding_INotify"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <!--<StackPanel>
            <Label>Name:</Label>
            <TextBox Text="{Binding Path=Name}" Height="30">         
            </TextBox>
            <Label>Age:</Label>
                <TextBox  Text="{Binding Path=Age}" Height="30">
            </TextBox>
            <Button Click="Button_Click">Change</Button>
        </StackPanel>-->
        <StackPanel>
            <Label>Name:</Label>
            <TextBox Text="" Height="30">
            </TextBox>
            <Label>Age:</Label>
            <TextBox  Text="" Height="30">
            </TextBox>
            <Button Click="Button_Click">Save</Button>
        </StackPanel>
        <ListBox x:Name="groupStudents">
            <ListBox.Items>
                <ListBoxItem>Andri 24</ListBoxItem>
                <ListBoxItem>Olga 18</ListBoxItem>
            </ListBox.Items>
        </ListBox>


    </StackPanel>
</Window>
